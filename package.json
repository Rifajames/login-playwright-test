{
  "name": "login-playwright-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "cucumber-js --format json:reports/cucumber_report.json --format junit:reports/junit_report.xml",
    "report": "node report-generator.js",
    "test:html": "npm run test && npm run report",
    "test:positive": "cucumber-js --tags @positive --format json:reports/cucumber_report.json --format junit:reports/junit_report.xml && node report-generator.js",
    "test:negative": "cucumber-js --tags @negative --format json:reports/cucumber_report.json --format junit:reports/junit_report.xml && node report-generator.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cucumber/cucumber": "^11.3.0",
    "multiple-cucumber-html-reporter": "^3.9.3",
    "playwright": "^1.53.2"
  }
}